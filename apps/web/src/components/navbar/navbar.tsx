"use client"

import { TopNavbar, TopNavbarTitle } from "sigma-ui"

function Navbar() {
  return (
    <TopNavbar>
      <TopNavbarTitle>SIGMA-UI</TopNavbarTitle>
    </TopNavbar>
  )
}

export default Navbar
